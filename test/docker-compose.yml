app:
    # image: eeacms/virtuoso_test:dev
    build: .
    #ports: # App accessible on http://localhost:4000
    #    - 4000:4000
    volumes:
        - ./:/test_results
    #    - ./app/:/code/:ro
    #environment: # Use current upstream staging instance
    #    - virtuoso_port=80
    #    - NODE_ENV=dev
    command: http://semantic.eea.europa.eu/sparql /test_results/query_results.csv
